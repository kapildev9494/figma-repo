import { Meta } from '@storybook/addon-docs';
import React from 'react';

<Meta title="Delish/About Delish" order = {2}/>

# Delish design system

This package uses **FluentUI** for building initial styling. The theme has been updated to match our brand theme by updating the tokens in `ThemeProvider`. We aim to significantly reduce development time, ensure a cohesive user experience, and maintain a high level of quality in our projects.